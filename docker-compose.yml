version: "3.9"

services:
  web:
    build: .
    volumes:
      - .:/app
    ports:
      - 8000:8000

  # db:
  #   image: postgres
  #   restart: always
  #   environment:
  #     POSTGRES_PASSWORD: postgres
  #   ports:
  #     - "5432:5432"
