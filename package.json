{
  "scripts": {
    "build": "tailwindcss build -i frontend/site.css -c frontend/tailwind.config.js -o static/site.css",
    "watch": "tailwindcss build -i frontend/site.css -c frontend/tailwind.config.js -o static/site.css --watch"
  },
  "engines": {
    "node": "16.x"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.2",
    "postcss": "^8.4.6",
    "tailwindcss": "^3.0.23"
  }
}
